pre-commit:
  parallel: true
  jobs:
    - name: biome
      glob: "*.{js,ts,jsx,tsx,json}"
      run: bun run clean {staged_files} && git add {staged_files}
    - name: typecheck
      run: bun run typecheck
