{
  "name": "xapxap-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "clean": "biome check --write --unsafe",
    "typecheck": "cd apps/mobile && bun run typecheck"
  }
}
